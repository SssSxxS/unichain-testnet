// Restart the program after making changes

export const ETHEREUM_SEPOLIA_RPC_URL = "https://ethereum-sepolia-rpc.publicnode.com"
export const UNICHAIN_SEPOLIA_RPC_URL = "https://sepolia.unichain.org"

export const BRIDGE_AMOUNT_RANGE = [0.001, 0.01] // [min, max] amount in ETH to transfer for bridge calls
export const SLEEP_BRIDGE = [100, 300] // [min, max] delay between bridge calls in seconds

export const SLEEP_MINT = [100, 300] // [min, max] delay between mint calls in seconds
